{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main technology-article" role="main">

        <div class="sidebar">
            <h3>Andra Teknologier</h3>
            <ul>
            {% for page in pages("technology") %}
                <li><a href="{{ page.url }}">
                    {{ page.title }}
                </a></li>
            {% endfor %}
            </ul>
        </div>

        <div class="article">
            <h2>{{ meta.title }}</h2>
            {{ content }}
        </div>
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>
